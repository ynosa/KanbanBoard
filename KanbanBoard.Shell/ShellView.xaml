<UserControl x:Class="KanbanBoard.Shell.ShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://www.codeplex.com/prism"
    mc:Ignorable="d">

    <!--<UserControl.Resources>
        <ResourceDictionary Source="/KanbanBoard.Shell;component/Styles/Styles.xaml"/>
    </UserControl.Resources>-->

    <Grid x:Name="LayoutRoot" Background="#666666">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="9*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0">
            <ContentControl x:Name="HeaderRegion" prism:RegionManager.RegionName="HeaderRegion" HorizontalAlignment="Right" VerticalAlignment="Center"/>
        </Border>

        <Border Grid.Row="1">
            <ContentControl x:Name="MainRegion" prism:RegionManager.RegionName="MainRegion" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>