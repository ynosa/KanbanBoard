<UserControl x:Class="KanbanBoard.Shell.ShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://www.codeplex.com/prism"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400">

    <Grid x:Name="LayoutRoot" Background="LightGreen" Height="300" Width="400">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0">
            <ContentControl x:Name="HeaderRegion" prism:RegionManager.RegionName="HeaderRegion"/>
        </Border>

        <Border Grid.Row="1">
            <ContentControl x:Name="MainRegion" prism:RegionManager.RegionName="MainRegion"/>
        </Border>
    </Grid>
</UserControl>
