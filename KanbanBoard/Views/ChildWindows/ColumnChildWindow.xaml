<controls:ChildWindow x:Class="KanbanBoard.Views.ChildWindows.ColumnChildWindow"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
           xmlns:controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls"
           Title="ColumnChildWindow">
    <Grid x:Name="LayoutRoot" Margin="2" Background="#333333">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock TextAlignment="Center" Foreground="White">column name</TextBlock>
            <TextBox Width="308" x:Name="columnName"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="oKButton" Content="OK"/>
            <Button x:Name="cancelButton" Content="Cancel"/>
        </StackPanel>
    </Grid>
</controls:ChildWindow>