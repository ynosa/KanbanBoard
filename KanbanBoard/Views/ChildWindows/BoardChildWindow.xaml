<controls:ChildWindow x:Class="KanbanBoard.Views.ChildWindows.BoardChildWindow"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
           xmlns:controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls">
    <Grid x:Name="LayoutRoot" Margin="2" Background="#333333">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock TextAlignment="Center">Board Name</TextBlock>
            <TextBox Width="308" x:Name="boardName"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="oKButton" Content="OK"/>
            <Button x:Name="cancelButton" Content="Cancel"/>
        </StackPanel>
    </Grid>
</controls:ChildWindow>