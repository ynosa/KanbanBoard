<UserControl x:Class="KanbanBoard.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="Login" Background="White" Height="100" Width="400">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Grid.Row="0" Text="Username"/>
        <TextBox Grid.Column="1" Grid.Row="0" x:Name="UserName" Text="{Binding UserName, Mode=OneWay}"/>

        <TextBlock Grid.Column="0" Grid.Row="1" Text="Password"/>
        <PasswordBox Grid.Column="1" Grid.Row="1" x:Name="Password" Password="{Binding Password, Mode=OneWay}" PasswordChar="#"/>

        <Button Grid.ColumnSpan="2" Grid.Row="2" Height="25" Content="Login" Command="{Binding LoginCommand}"/>
    </Grid>
</UserControl>
